<% if current_user.admin? %>
    <h1>admin/users/:id/edit</h1>
    <%= form_for [:admin, @user] do |f| %>
        <%= render partial: 'users/edit', locals: {f: f, user: @user} %>
        <div><%= f.submit 'Submit' %></div>
    <% end %>
<% else %>
    <h1>No Access</h1>
<% end %>