<% if current_user.admin? %>
    <%= form_for [:admin, @user] do |f| %>
        <%= render partial: 'users/new', locals: {f: f, user: @user} %>
        <div><%= f.submit 'Submit' %></div>
    <% end %>
<% else %>
    <h1>No Access</h1>
<% end %>